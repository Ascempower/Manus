# Environment Variables Template
# Copy this file to .env.local for local development
# For production, set these in Netlify Dashboard → Site Settings → Environment Variables

# VGS (Very Good Security) Configuration
# These are required for secure form handling
VGS_VAULT_ID=your_vgs_vault_id_here
VGS_ROUTE_ID=your_vgs_route_id_here

# Optional: VGS Client Credentials (if using server-side VGS API)
# VGS_CLIENT_ID=your_vgs_client_id_here
# VGS_CLIENT_SECRET=your_vgs_client_secret_here

# Next.js Configuration
# NEXT_PUBLIC_ variables are exposed to the browser
# NEXT_PUBLIC_SITE_URL=https://your-domain.com